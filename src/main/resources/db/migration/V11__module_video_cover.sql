-- Обложка вводного видео модуля (постер до запуска, как у уроков).
-- Ключ объекта в S3 или полный URL.
-- Старые картинки модулей остаются в image_url — это превью карточки
-- на главном экране, переносить их никуда не нужно.
ALTER TABLE course_module
    ADD COLUMN video_cover_url TEXT;
